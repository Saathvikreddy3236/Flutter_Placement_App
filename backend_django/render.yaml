services:
  - type: web
    name: flutter-project-backend
    env: python
    rootDir: backend_django
    buildCommand: bash build.sh
    startCommand: gunicorn backend_django.wsgi:application
    envVars:
      - key: DEBUG
        value: False
      - key: ALLOWED_HOSTS
        value: flutter-project-backend.onrender.com
